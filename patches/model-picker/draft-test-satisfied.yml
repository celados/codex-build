id: codex-build-model-picker-draft-test-satisfied
language: Rust
message: The model picker shortcut preserves a non-empty draft in tests.
rule:
  pattern: |-
    async fn model_picker_filters_hidden_models_and_shortcut_preserves_draft() {
        $$$BODY
    }
