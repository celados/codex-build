id: codex-build-model-picker-default-binding-satisfied
language: Rust
message: Alt+P is the default model picker binding.
rule:
  all:
    - pattern: ChatKeymap { $$$FIELDS }
    - has:
        pattern: "default_bindings![alt(KeyCode::Char('p'))]"
        stopBy: end
