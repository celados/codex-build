id: has-generalized-mailbox-input-type
language: rust
rule:
  pattern: |
    enum PendingMailboxInput {
        $$$BEFORE
        StandaloneOutput {
            item: ResponseItem,
            _diagnostics_guard: GaugeGuard,
        },
        $$$AFTER
    }
