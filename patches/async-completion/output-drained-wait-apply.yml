id: await-durable-output-drained-signal
language: rust
rule:
  pattern: $OUTPUT_DRAINED.notified().await
  inside:
    pattern: pub(crate) fn spawn_exit_watcher($$$PARAMS) { $$$BODY }
    stopBy: end
fix: $OUTPUT_DRAINED.cancelled().await
constraints:
  OUTPUT_DRAINED:
    regex: ^output_drained$
