id: checks-generalized-mailbox-storage
language: rust
rule:
  pattern: |
    pub(crate) async fn has_pending_mailbox_items(&self) -> bool {
        !self.mailbox_pending_items.lock().await.is_empty()
    }
