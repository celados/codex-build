id: codex-build-reasoning-shortcut-confirmation-gate-satisfied
language: Rust
message: Reasoning shortcuts use provider confirmation metadata.
rule:
  pattern: |-
    if direction == ReasoningShortcutDirection::Raise
        && Self::reasoning_effort_requires_confirmation(&preset, &next_effort)
    {
        $$$BODY
    }
