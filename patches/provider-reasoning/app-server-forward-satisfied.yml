id: codex-build-app-server-reasoning-confirmation-forward-satisfied
language: Rust
message: App-server model projection preserves confirmation metadata.
rule:
  pattern: |-
    ReasoningEffortOption {
        reasoning_effort: preset.effort,
        description: preset.description,
        requires_confirmation: preset.requires_confirmation,
    }
