id: codex-build-app-server-reasoning-confirmation-fixture-satisfied
language: Rust
message: model/list expectations retain confirmation metadata.
rule:
  pattern: |-
    ReasoningEffortOption {
        reasoning_effort: preset.effort.clone(),
        description: preset.description.clone(),
        requires_confirmation: preset.requires_confirmation,
    }
